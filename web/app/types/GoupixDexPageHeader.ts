// app/types/GoupixDexPageHeader.ts

/**
 * Props for the GoupixDexPageHeader component.
 */
export type GoupixDexPageHeaderProps = {
  /** Mono uppercase eyebrow above the title (usually the sidebar group name). */
  eyebrow?: string
  /** Page title. */
  title: string
  /** One-line description under the title. */
  description?: string
}
