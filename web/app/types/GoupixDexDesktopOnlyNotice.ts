// app/types/GoupixDexDesktopOnlyNotice.ts

/**
 * Props for the GoupixDexDesktopOnlyNotice component.
 */
export type GoupixDexDesktopOnlyNoticeProps = {
  /** Name of the feature that needs the desktop app. */
  featureLabel: string
  /** Optional extra sentence explaining why the desktop app is required. */
  reason?: string
}
