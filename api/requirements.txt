# HTTP / API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0

# Database
SQLAlchemy>=2.0.0
pymysql>=1.1.0

# Auth (bcrypt only — avoids passlib vs bcrypt>=4.1 __about__ breakage)
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# Existing integrations
nodriver>=0.48.0
python-dotenv>=1.0.0
httpx>=0.27.0
Pillow>=10.0.0
supabase>=2.0.0

# Vinted wardrobe sync (catalogue + cookies navigateur)
requests>=2.31.0
browser-cookie3>=0.19.0
vinted-scraper>=3.0.0

# Shipping label PDF generation (Avery L7173 — 8 labels per A4)
reportlab>=4.0.0
